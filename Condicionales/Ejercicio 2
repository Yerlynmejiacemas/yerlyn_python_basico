#Escribir una función que reciba una cadena de caracteres contraseña en una variable,
#pregunte al usuario por la contraseña e imprima por pantalla si la contraseña introducida
#por el usuario coincide con la guardada en la variable sin tener en cuenta mayúsculas y
#minúsculas.


HARD_PASSWORD = "Elgasto**@yerlyn"
password = input("cual es tu password?")

while password != HARD_PASSWORD:
 print("password incorrecto")
 password = input("escribe tu password otra vez")
 
print("tu password es correcto!")

